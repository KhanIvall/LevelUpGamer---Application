package com.example.level_up_gamer.ui.theme

import androidx.compose.ui.graphics.Color

// 1. Fondo "Gamer" (No usamos negro absoluto #000000 porque cansa la vista, usamos gris muy oscuro)
val FondoDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E2E) // Un gris azulado para las "Tarjetas" o items

// 2. Color Principal (Tu Cyan Gamer)
val NeonCyan = Color(0xFF00E5FF)
val NeonCyanDark = Color(0xFF00B8D4) // Una versión un poco más oscura para contrastes

// 3. Color Secundario (Un morado o rosa va muy bien con el Cyan)
val NeonPurple = Color(0xFF9565EE)

// 4. Textos
val TextoBlanco = Color(0xFFEEEEEE) // Blanco suave
val TextoGris = Color(0xFFB0BEC5)   // Para subtítulos